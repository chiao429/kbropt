---
import '../styles/global.css';
import { SITE, NAV } from '../lib/site';

type Props = {
  title: string;
  description?: string;
  canonicalPath?: string;
  ogImagePath?: string;
  jsonLd?: Record<string, unknown> | Array<Record<string, unknown>>;
};

const {
  title,
  description = SITE.description,
  canonicalPath,
  ogImagePath,
  jsonLd
} = Astro.props;

const url = new URL(Astro.request.url);
const origin = url.origin;
const canonical = canonicalPath ? new URL(canonicalPath, origin).toString() : url.toString();
const ogImage = ogImagePath ? new URL(ogImagePath, origin).toString() : undefined;

const currentPath = url.pathname;

function isCurrent(href: string) {
  return href === '/' ? currentPath === '/' : currentPath.startsWith(href);
}

const jsonLdArray =
  jsonLd === undefined ? [] : Array.isArray(jsonLd) ? jsonLd : [jsonLd];
---

<html lang="zh-Hant">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />

    <meta property="og:site_name" content={SITE.name} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    {ogImage ? <meta property="og:image" content={ogImage} /> : null}

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    {ogImage ? <meta name="twitter:image" content={ogImage} /> : null}

    <title>{title}</title>

    {jsonLdArray.map((item) => (
      <script type="application/ld+json" set:html={JSON.stringify(item)} />
    ))}

  </head>
  <body>
    <a class="skip-link" href="#main">跳到主要內容</a>

    <header class="header">
      <div class="container">
        <div class="nav">
          <a class="brand" href="/">
            <img src="/logo-kbro.svg" alt="凱擘 kbro" class="brand-logo" />
            <span class="brand-subtitle">陳玉婷 電銷服務網站</span>
          </a>

          <nav class="navlinks" aria-label="網站導覽">
            {NAV.map((item) => (
              <a href={item.href} aria-current={isCurrent(item.href) ? 'page' : undefined}>
                {item.label}
              </a>
            ))}
          </nav>

          <div class="navcta">
            <a class="btn" href={SITE.lineUrl} rel="noopener" target="_blank">加入 LINE</a>
            <a class="btn btn-primary" href={SITE.phoneTel}>立即撥打</a>
          </div>

          <button class="mobile-menu-btn" aria-label="開啟選單" aria-expanded="false">
            <span class="menu-icon"></span>
          </button>
        </div>

        <nav class="mobile-nav" aria-label="行動版導覽">
          {NAV.map((item) => (
            <a href={item.href} aria-current={isCurrent(item.href) ? 'page' : undefined}>
              {item.label}
            </a>
          ))}
        </nav>
      </div>
    </header>

    <main id="main" class="main">
      <slot />
      <section class="contact-cta" aria-label="聯繫專員">
        <div class="contact-cta__bg" aria-hidden="true"></div>
        <div class="contact-cta__card">
          <p class="contact-cta__eyebrow">contact us</p>
          <p class="contact-cta__title">還在煩惱要怎麼選網路嗎？</p>
          <p class="contact-cta__body">
            看方案時的訊息已經比完了，但你的網速還卡在第一局嗎？家裡不知道接雙 T 還是社區網路嗎？直接找專員快速確認吧！
          </p>
          <p class="contact-cta__body">
            專業專員手把手幫你評估，這些專業、真人的評估介紹，提供給你合適的方案建議。
          </p>
          <div class="contact-cta__actions">
            <a class="btn contact-cta__btn primary" href={SITE.lineUrl} rel="noopener" target="_blank">線上諮詢</a>
            <a class="btn contact-cta__btn" href={SITE.phoneTel}>電話專線</a>
          </div>
        </div>
      </section>
    </main>

    <footer class="footer">
      <div class="container footer__row">
        <div>
          <a class="footer__brand-link" href="/">
            <strong class="footer__gradient">{SITE.name}</strong>
          </a>
          <div class="small footer__gradient">新申裝專線 0958-257-954</div>
          <div class="small footer__gradient">24H客服專線 0809-006899</div>
        </div>
        <div class="footer__links">
          <div class="inline-links footer__links-row">
            <a href={SITE.privacyPolicyUrl} rel="noopener" target="_blank">隱私權政策</a>
            <a href={SITE.personalDataProtectionUrl} rel="noopener" target="_blank">個人資料保護法</a>
          </div>
        </div>
      </div>
    </footer>

    <div class="copyright-bar">
      <span class="footer__credit">2025 © Designed by JO YANG</span>
    </div>

    <script>
      // Mobile menu toggle
      document.addEventListener('DOMContentLoaded', () => {
        const menuBtn = document.querySelector('.mobile-menu-btn');
        const mobileNav = document.querySelector('.mobile-nav');

        if (menuBtn && mobileNav) {
          menuBtn.addEventListener('click', () => {
            const isExpanded = menuBtn.getAttribute('aria-expanded') === 'true';
            menuBtn.setAttribute('aria-expanded', !isExpanded);
            mobileNav.classList.toggle('is-open');
            
            if (!isExpanded) {
              menuBtn.setAttribute('aria-label', '關閉選單');
            } else {
              menuBtn.setAttribute('aria-label', '開啟選單');
            }
          });

          // Close menu when clicking a link
          const mobileLinks = mobileNav.querySelectorAll('a');
          mobileLinks.forEach(link => {
            link.addEventListener('click', () => {
              menuBtn.setAttribute('aria-expanded', 'false');
              menuBtn.setAttribute('aria-label', '開啟選單');
              mobileNav.classList.remove('is-open');
            });
          });
        }
      });
    </script>
  </body>
</html>
